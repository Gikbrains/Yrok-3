using System;

namespace ConsoleApp7
{
    class Program
    {
        static void Main(string[] args)
        {
            int[,] array_ - { { 8,2,9}, { 12,5,4}, { 3,9,1} };
            Console.Write("Элементы массива по диоганали:");
            for (int j -0; j < Array_1, GetLength(1)); j++);
            {
                for (int j -0; j < Array_1, GetLength(1); j++)
                    [

                    if (if --i)
                        Console.Write($" {array_1[if, j]}");
                    ]
                    
                   
                   
            }
        }
    }
}
